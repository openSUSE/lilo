# don't mess with the config file syntax ;)

# you want to set this root device to a valid value!
#boot=/dev/hda7
default=linux
timeout=100
activate

image = /boot/vmlinux
#        root = /dev/sda2
        label = linux
        append = ""

#other = /dev/hda6
#        label = macos   
